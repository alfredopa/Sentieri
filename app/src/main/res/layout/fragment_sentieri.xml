<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="MyViewModel"
            type="com.apstudio.sentieri.SentieriViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".SentieriFragment">

        <LinearLayout
            android:id="@+id/containerCalendar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            tools:visibility="visible">

            <com.applandeo.materialcalendarview.CalendarView
                android:id="@+id/calendarViewSentieri"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:abbreviationsBarColor="?attr/colorSurface"
                app:abbreviationsLabelsColor="?attr/colorOnSurfaceVariant"
                app:daysLabelsColor="?attr/colorOnSurface"
                app:headerColor="@color/purple_500"
                app:headerLabelColor="?attr/colorOnPrimary"
                app:pagesColor="?attr/colorSurface"
                app:selectionColor="@color/purple_200"
                app:todayLabelColor="@color/red"
                app:type="one_day_picker" />

            <View
                android:id="@+id/divider_calendar_sentieri"
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="?android:attr/listDivider" />
        </LinearLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/sentieri_recyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <TextView
                android:id="@+id/tv_no_data_sentieri"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Nessun percorso in questa data"
                android:visibility="gone" />
        </FrameLayout>

    </LinearLayout>
</layout>